import PlaceholderPage from '@components/layout/PlaceholderPage';

export const metadata = { title: 'AI Agent' };

export default function AiAgentPage() {
  return (
    <PlaceholderPage
      icon="cpu"
      title="CoHub AI Agent"
      description="Chatbot tư vấn HLV bằng AI — đề xuất phù hợp với mục tiêu của bạn. Sẽ build sau khi MVP ổn định."
    />
  );
}
