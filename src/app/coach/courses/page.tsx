import CoachCoursesClient from '@features/coach-cms/CoachCoursesClient';

export const metadata = { title: 'Khoá học của tôi (HLV)' };

export default function CoachCoursesPage() {
  return <CoachCoursesClient />;
}
