import CoachSessionsClient from '@features/coach-cms/CoachSessionsClient';

export const metadata = { title: 'Lịch dạy mở (HLV)' };

export default function CoachSessionsPage() {
  return <CoachSessionsClient />;
}
