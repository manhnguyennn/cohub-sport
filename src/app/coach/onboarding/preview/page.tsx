import OnboardingPreview from '@features/onboarding/OnboardingPreview';

export const metadata = { title: 'Xem preview profile' };

export default function CoachOnboardingPreviewPage() {
  return <OnboardingPreview />;
}
