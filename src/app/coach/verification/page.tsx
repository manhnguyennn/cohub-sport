import CoachVerificationClient from '@features/onboarding/CoachVerificationClient';

export const metadata = { title: 'Xác minh — Verified Coach' };

export default function CoachVerificationPage() {
  return <CoachVerificationClient />;
}
