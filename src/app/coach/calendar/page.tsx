import CoachCalendarClient from '@features/coach-cms/CoachCalendarClient';

export const metadata = { title: 'Lịch HLV' };

export default function CoachCalendarPage() {
  return <CoachCalendarClient />;
}
